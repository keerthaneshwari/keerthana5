#include<stdio.h>
#include<conio.h>
int main()
{
 char c;
 printf("\nenter an alphabet");
 scanf("%c",&c);
 if(c>=a && c>=z)||(c<=A) && c<=Z)
 {
  printf("%c is an alphabet",c);
  else
  printf("%c is not an alphabet",c);
  }
  return 0;
  getch();
  }
  
